Experiments with Android ARCore from the distant 2020

The application allows you to place 3D objects into the real environment using the phone’s camera.
To use it, add a 3D model file (.sfb) to the assets folder.
